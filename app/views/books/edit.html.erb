<html>
<head>
  <title>Bookers</title>
  <link rel="stylesheet" media="all" href="/bookers/app/assets/stylesheets/application.css">
</head>


<body>
<h1>Editing Book</h1>
<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved:
<ul>
<% @book.errors.full_messages.each do |message| %>
<li><%= message %></li>
<% end %>
</ul>
<% end %>

<%= form_with model:@book, url:"/books/#{@book.id}", local:true do |f| %>
<lavel class="book-title">Title</lavel>
<div class="in_field">

<%= f.text_field :title %>

</div>
<lavel class="book-body">Body</lavel>
<div class="in_field">

<%= f.text_area :body %>

</div>

<div class="actions">
<%= f.submit 'Update Book' %>

</div>
<% end %>

<%= link_to "Show", book_path(@book.id) %> | <%= link_to "Back",  books_path %>

</body>
</html>